# bash-redirect-checker

```
user$ sudo -i
root$ mkdir /opt/bash-redirect-checker && \
git clone https://github.com/bjoern-hempel/bash-redirect-checker.git /opt/bash-redirect-checker/. && \
ln -s /opt/bash-redirect-checker/bin/check /usr/local/bin/redirect-checker
```
